### 권한문제가 있는 경우
```
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser -Force
```

### pyenv 설치 (파이썬의 여러버전을 선택하여 사용할 수 있도록 해주는 도구)
```
git clone https://github.com/pyenv-win/pyenv-win.git "$env:USERPROFILE\.pyenv"
```

### 환경변수 추가
```
[System.Environment]::SetEnvironmentVariable('PYENV', $env:USERPROFILE + "\.pyenv\pyenv-win\", "User")
[System.Environment]::SetEnvironmentVariable('PYENV_ROOT', $env:USERPROFILE + "\.pyenv\pyenv-win\", "User")
[System.Environment]::SetEnvironmentVariable('PYENV_HOME', $env:USERPROFILE + "\.pyenv\pyenv-win\", "User")
```
```
[System.Environment]::SetEnvironmentVariable('PATH', $env:USERPROFILE + "\.pyenv\pyenv-win\bin;" + $env:USERPROFILE + "\.pyenv\pyenv-win\shims;" + [System.Environment]::GetEnvironmentVariable('PATH', "User"), "User")
```

### pyenv 정상설치 확인
```
pyenv
```

### pyenv-doctor로 설치 확인(윈도우에서는 설치안됨)
```
git clone https://github.com/pyenv/pyenv-doctor.git "$(pyenv root)/plugins/pyenv-doctor"
pyenv doctor
```

### 설치 가능한 파이썬 버전 목록 확인
```
pyenv install --list
```

### 특정 버전 파이썬 설치
```
pyenv install 3.14.2
```

### 설치된 파이썬 버전 확인
```
pyenv versions
```

### 설치한 파이썬을 전역으로 설정
```
pyenv global 3.14
```

### 개별 프로젝트 폴더에서만 설정
```
pyenv local 3.14
```

### 현재 터미널 세션에서만 적용하려면
```
pyenv shell 3.14
```

### 파이썬 설치 버전 확인
```
python --version
```

### pyenv-virtualenv 플러그인으로 가상환경 지원(윈도우에서는 설치 안됨)
```
git clone https://github.com/pyenv/pyenv-virtualenv.git "$(pyenv root)/plugins/pyenv-virtualenv"
```

### 가상환경 생성(myenv): 윈도우에서는 실행안됨
```
pyenv virtualenv 3.14 myenv
```

### 윈도우에서 가상환경 생성(venv 또는 virtualenv 사용)
```
python -m venv myenv
```
```
pip install virtualenv
virtualenv myenv
```
### 가상환경 활성화 및 비활성화 

```
myenv\Scripts\activate
```
```
deactivate
```

### 특정 프로젝트에 가상환경 자동 적용 (윈도우에서는 실행안됨)
```
pyenv local myenv
```

### poetry 설치 (파이썬 패키지 의존성 관리도구)
```
pip3 install poetry
```

### poetry 2.0 이상에서 shell 명령어 사용하기 (파이썬 가상환경 사용 및 패키지 설치)
```
poetry self add poetry-plugin-shell
```

### 파이썬 패키지를 최신 버전으로 일괄 업데이트
```
poetry update
```
### 설치된 패키지 확인
```
pip list
```
